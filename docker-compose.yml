version: '3.8'

services:
  todo-app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./todos.json:/app/todos.json  # Persist data
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped